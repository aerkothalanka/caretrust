# CareSignal Frontend

React/Vite frontend based on Design 1 Civic Clarity.

```bash
npm install
npm run dev
npm run build
```

The app calls `/api/*` endpoints and falls back to bundled demo data when the backend is unavailable.
